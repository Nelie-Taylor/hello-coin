services:
  dashboard:
    image: ghcr.io/nelie-taylor/hello-coin:latest
    build: .
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
